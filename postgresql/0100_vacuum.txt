-- Production rule: Alert on transactions open > 5 min, inactive replication slots, and backend_xmin age > 100M XIDs.

-- who is holding the xmin horizon back?
SELECT pid, usename, state, backend_xmin,
       now() - xact_start AS tx_age, query
FROM pg_stat_activity
WHERE backend_xmin IS NOT NULL
ORDER BY backend_xmin ASC
LIMIT 10;

-- replication slots
SELECT slot_name, active, age(xmin) AS xmin_age
FROM pg_replication_slots
ORDER BY xmin_age DESC;


-- per table override for hot tables
ALTER TABLE events SET (
  autovacuum_vacuum_scale_factor = 0.01,
  autovacuum_vacuum_threshold = 10000,
  autovacuum_vacuum_cost_limit = 4000
);



# postgresql.conf
autovacuum_vacuum_scale_factor = 0.05
autovacuum_vacuum_cost_limit = 2000
autovacuum_vacuum_cost_delay = 2
autovacuum_max_workers = 10
autovacuum_freeze_max_age = 100000000
maintenance_work_mem = 512MB

SETTING	                     DEFAULT	EFFECT	RECOMMENDED
autovacuum_vacuum_scale_factor	0.2	       Percentage of dead tuples required to trigger vacuum	0.02–0.05 for large tables
autovacuum_vacuum_cost_limit	200	       I/O budget per vacuum cycle	2000–4000 on modern SSDs
autovacuum_vacuum_cost_delay	2ms	       Pause between vacuum cycles	2ms (usually fine)
autovacuum_naptime	              1min	       How often the launcher wakes up	15s for busy systems
autovacuum_max_workers	       3	       Number of concurrent vacuum workers	5–10 on multi‑core servers
autovacuum_freeze_max_age	       200,000,000	Anti‑wraparound threshold	100,000,000 on busy systems
maintenance_work_mem	              64MB	       Memory per worker for dead tuple processing	512MB–2GB
vacuum_freeze_min_age	       50,000,000	Age at which tuples become eligible for freezing	Keep default unless emergency
